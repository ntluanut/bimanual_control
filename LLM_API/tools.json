[
{
  "type": "function",
  "name": "arm_control",
  "description": "move arm to target coordinates",
  "parameters": {
    "type": "object",
    "properties": {
      "arm": {
        "type": "string",
        "enum": ["left", "right"],
        "description": "choose arm to move"
      },
      "position": {
        "type": "array",
        "items": {"type": "number"},
        "minItems": 3,
        "maxItems": 3
      },
      "orientation": {
        "type": "array",
        "items": {"type": "number"},
        "minItems": 4,
        "maxItems": 4
      }
    },
    "required": ["arm", "position", "orientation"]
  }
}
]